class Project {
  constructor(id, catId, title, picture, Url) {
    this.id = id;
    this.catId = catId;
    this.title = title;
    this.picture = picture;
    this.Url = Url;
  }
}

export default Project;
